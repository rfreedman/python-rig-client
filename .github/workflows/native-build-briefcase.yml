name: Build Native Executable

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-windows:
    name: Build Windows Executable
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      - name: Cleanup previous dist
        run: |
          if (Test-Path -Path ./dist) { Remove-Item -Recurse -Force ./dist }

      - name: Cache pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install briefcase==0.3.25
          pip install -r requirements.txt

      - name: Build Windows Executable
        run: |
          briefcase create
          briefcase build
          briefcase package

      - name: Zip Windows build
        run: |
          $zipName="rigclient-windows-${{ github.run_number }}.zip"
          Compress-Archive -Path ./dist/* -DestinationPath $zipName

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: rigclient-windows-${{ github.run_number }}
          path: rigclient-windows-${{ github.run_number }}.zip

  build-macos:
    name: Build macOS App
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      - name: Cleanup previous dist
        run: rm -rf ./dist

      - name: Cache pip
        uses: actions/cache@v3
        with:
          path: ~/Library/Caches/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install briefcase==0.3.25
          pip install -r requirements.txt

      - name: Build macOS App
        run: |
          briefcase create
          briefcase build
          briefcase package

      - name: Zip macOS build
        run: |
          zipName="rigclient-macos-${{ github.run_number }}.zip"
          zip -r "$zipName" ./dist/*

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: rigclient-macos-${{ github.run_number }}
          path: rigclient-macos-${{ github.run_number }}.zip

  build-linux:
    name: Build Linux Executable
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.12

      - name: Cleanup previous dist
        run: rm -rf ./dist

      - name: Cache pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install briefcase==0.3.25
          pip install -r requirements.txt

      - name: Build Linux Executable
        run: |
          briefcase create
          briefcase build
          briefcase package

      - name: Zip Linux build
        run: |
          zipName="rigclient-linux-${{ github.run_number }}.zip"
          zip -r "$zipName" ./dist/*

      - name: Upload Linux artifact
        uses: actions/upload-artifact@v4
        with:
          name: rigclient-linux-${{ github.run_number }}
          path: rigclient-linux-${{ github.run_number }}.zip
